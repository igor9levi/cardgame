import React, { PureComponent } from 'react';
import './Board.css';

export default class Board extends PureComponent {
  render() {
    return (
      <div className="board">Hello React</div>
    );
  }
}
